# URL base del backend (sin barra final). Si está vacío, en desarrollo se usa el proxy /api de Vite.
# Ejemplo producción: https://api.mi-dominio.com
VITE_API_BASE_URL=

# Identificador opcional de la pistola lectora (se puede sobrescribir en el formulario).
VITE_SCANNER_DEVICE_ID=
